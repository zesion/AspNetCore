dotnet-add-servicereference
================

`dotnet-add-servicereference` is a command line tool to add service references to an ASP.NET Core project.

### How To Use

Run `dotnet add servicereference --help` for more information about usage.
